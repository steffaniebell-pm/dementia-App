export const buildMedicationReminderMessage = (): string => {
  return 'Medication time';
};

export const buildEscalationMessage = (): string => {
  return 'Check in needed: medication reminder not acknowledged yet.';
};